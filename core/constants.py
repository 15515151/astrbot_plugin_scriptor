# core/constants.py
"""Scriptor 插件全局常量定义"""

# ==========================================
# 时间相关常量（秒）
# ==========================================
GROUP_INACTIVE_THRESHOLD = 600  # 群组不活跃阈值：10 分钟
SESSION_TIMEOUT = 3600  # 会话超时：1 小时
BACKUP_INTERVAL = 86400  # 备份间隔：1 天
SYNC_INTERVAL = 259200  # 同步间隔：3 天
REBUILD_INTERVAL = 604800  # 重建间隔：7 天
SCHEDULER_BACKUP_INTERVAL = 20 * 3600  # 调度器备份间隔：20 小时

# ==========================================
# 消息数量阈值
# ==========================================
MESSAGE_PREVIEW_LENGTH = 50  # 消息预览长度
RECENT_MESSAGES_LIMIT = 20  # 最近消息数量限制
CONTEXT_MESSAGES_LIMIT = 100  # 上下文消息数量限制
SUDO_AUDIT_LOG_MAX = 1000  # Sudo 审计日志最大数量

# ==========================================
# 内容长度阈值
# ==========================================
CONTENT_PREVIEW_LENGTH = 500  # 内容预览长度
CONTENT_TRUNCATE_LENGTH = 150  # 内容截断长度
SUMMARY_MAX_LENGTH = 300  # 摘要最大长度
KNOWLEDGE_BASE_CONTENT_MAX = 1000  # 知识库内容最大长度
MEMORY_CONTENT_MAX = 500  # 记忆内容最大长度

# ==========================================
# 文件大小阈值（字节）
# ==========================================
MAX_FILE_SIZE = 1024 * 1024  # 最大文件大小：1 MB
MAX_NODE_SIZE = 5000  # 最大节点大小
MIN_NODE_SIZE = 1000  # 最小节点大小

# ==========================================
# 天数阈值
# ==========================================
TASK_EXPIRY_DAYS = 30  # 任务过期天数
DIARY_EXPIRY_DAYS = 30  # 日记过期天数

# ==========================================
# 检索指导提示词
# ==========================================
RETRIEVAL_GUIDANCE = (
    "\n\n【核心指令：记忆检索与决策】\n"
    "1. **检索优先**：在回答任何涉及事实、过去约定或他人信息的问题前，**必须先调用 `memory_search`**。\n"
    "   - scope 参数深度指南：\n"
    "     * personal：查询当前用户的私有偏好、习惯、秘密。\n"
    "     * group：查询当前群聊的公共事件、共同约定、群友互动。\n"
    "     * cross：当用户在私聊中询问「大家在聊什么」或「群里有什么事」时，**必须使用 cross**。系统会自动处理权限隔离。\n"
    "       **【重要】cross 搜索的意图检测：**\n"
    "       - 在私聊中使用 cross 时，用户需明确提及「所有群」、「其他群」、「跨群」等关键词\n"
    "       - 在群聊中使用 cross 时，用户需明确提及「我的私聊」、「私人记忆」、「私聊里」等关键词\n"
    "       若不满足上述条件，cross 将被系统强制降级为 personal 或 group\n"
    "2. **文档查阅**：涉及 Scriptor 插件自身功能、命令用法、配置项时，先调用 `usage_docs_search`。\n"
    "3. **主动铭记**：发现用户的新偏好（如「我不爱吃香菜」）、重要承诺或身份变化时，立即调用 `core_memory_remember`。记录时请包含具体上下文，避免模糊描述。\n"
    "4. **深度阅读**：如果 `memory_search` 返回的摘要不足以回答问题，使用 `note_recall` 查看完整的日记上下文。\n"
)
